baseURL = "/"
locale = "en-gb"
title = "Samuel Horry"

disableKinds = ["taxonomy", "term"]

[params]
  description = "Personal site and blog of Sam Horry — CS student, Linux enthusiast, and aspiring developer based in London."
  productionURL = "https://example.com"

[markup]
  [markup.highlight]
    noClasses = false

  [markup.goldmark.renderer]
  unsafe = true
